# 系统深入分析计划（基于证据 + 本地数据）

> 目标：把“文献中的方法与思路”转化为“对你的数据可执行的分析方案”，并形成可复现的分析流水线。
>
> 原则：一次只推进 1–3 个关键分析问题；分析稳定后再进入方法撰写。

## 0) 输入汇总

- 数据摘要卡：是否已完成（是/否）｜对应文件：
- 事实核查报告：是否已完成（是/否）｜对应文件：
- 目标期刊/风格：Current Biology（默认）｜草稿语言：中文（默认）

## 1) 研究问题拆解（最多 3 个）

1. 问题/假设：
   - 对应响应变量（y）：
   - 对应解释变量（x）：
   - 预期方向（可选）：

## 2) 文献方法对齐（每个问题至少 1 个依据）

> 把“引用”变成“可执行的设定”，而不是只写在参考文献里。

- 问题 1：推荐方法/模型：
  - 关键设定（分布族/链接/随机效应/距离度量/置换策略等）：
  - 适用边界（系统/尺度/数据类型）：
  - 引用占位符与 DOI（例如 `[R1]`）：

## 3) 数据落地与预处理

- 数据表连接/筛选规则（如多表）：
- 缺失值策略（删除/插补/模型处理）：
- 变量变换（log/标准化/hellinger 等）：
- 空间/时间对齐与去重规则（若适用）：

## 4) 分析实现（可复现）

- 分析语言与工具：R（默认；写明版本与关键包）
- 脚本路径与输出目录（建议）：
  - `analysis/02_deep_analysis/analysis.R`（默认）
  - `analysis/02_deep_analysis/output/`
- 关键输出物（必须明确）：
  - 核心统计量（效应量/CI/p 值/拟合优度等）
  - 关键图表（命名规则）
  - 诊断与稳健性检查（模型诊断/敏感性分析/替代设定）

## 5) 质量控制与“停止条件”

- 诊断通过的标准：
- 稳健性检查清单：
- 什么时候可以进入方法撰写（满足哪些证据与结果）：
